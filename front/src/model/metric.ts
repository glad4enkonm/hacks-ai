export interface IMetric {
    metricId?: bigint,
    calculationDate?: Date,
    loaction?: string,
    positiveOverNegative?: number,
    educationalDistance?: number,
    cityDistrictMetric?: number,
    reserectionalDistance?: number,
}
